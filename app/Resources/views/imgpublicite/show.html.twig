{% extends 'base.html.twig' %}

{% block body %}
    <h1>Imgpublicite</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imgPublicite.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ imgPublicite.url }}</td>
            </tr>
            <tr>
                <th>Alt</th>
                <td>{{ imgPublicite.alt }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{% if imgPublicite.statut %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_imgpublicite_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_imgpublicite_edit', { 'id': imgPublicite.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
