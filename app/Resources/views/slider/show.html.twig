{% extends 'base.html.twig' %}

{% block body %}
    <h1>Slider</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slider.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ slider.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ slider.description }}</td>
            </tr>
            <tr>
                <th>Publiepar</th>
                <td>{{ slider.publiePar }}</td>
            </tr>
            <tr>
                <th>Modifiepar</th>
                <td>{{ slider.modifiePar }}</td>
            </tr>
            <tr>
                <th>Publiele</th>
                <td>{{ slider.publieLe }}</td>
            </tr>
            <tr>
                <th>Modifiele</th>
                <td>{{ slider.modifieLe }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{% if slider.statut %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_slider_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_slider_edit', { 'id': slider.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
